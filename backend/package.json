{
  "name": "flex-reviews-backend",
  "version": "1.0.0",
  "main": "api/server.js",
  "type": "commonjs",
  "scripts": {
    "dev": "nodemon api/server.js",
    "start": "node api/server.js",
    "db:init": "psql -d flex -f sql/schema.sql",
    "db:seed": "psql -d flex -f sql/seed.sql"
  },
  "engines": {
    "node": "22.14"
  },
  "dependencies": {
    "@fastify/cors": "^8.5.0",
    "axios": "^1.12.2",
    "dotenv": "^16.4.1",
    "fastify": "^4.26.2",
    "psql": "^0.0.1",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
